# 머니빌리지 QA 시뮬레이션 결과 리포트 (2026-09-18)

## 1. 테스트 개요

| 항목 | 내용 |
|---|---|
| 테스트 일시 | 2026-09-18 |
| 환경 | 로컬 (`npm run dev` — Vite :5173 / Express+Socket.io :3001) |
| 브라우저 | Claude in Chrome (JS 직접 실행 기반 조작 — 아래 "테스터 제약" 참고) |
| 기준 제안서 | `proposal/20260831_qa_simulation_proposal.md` + `proposal/20260918_qa_simulation_proposal.md`(연결 끊김 검증 추가 요청) |
| 참고 스펙 | `docs/superpowers/specs/2026-08-24-block-registration-disconnected-design.md`, `docs/superpowers/specs/2026-08-03-disconnect-grace-period-design.md` |
| 이전 리포트 | `test-reports/20260831_qa_result.md` (ISSUE-1~7, 모두 조치 완료로 기록됨) |
| 생성한 테스트 데이터 | 기존 `qa_teacher` 계정 재사용, 신규 수업 "QA 0918 disconnect test", 팀 1(5B513E, 등록 완료 처리됨), 팀 2(934D47, 버그로 등록 대기 상태 남음) |

### 테스터 제약 (결과 해석 시 참고)

- **이번 세션은 예정보다 크게 축소되었다.** 최초 시도는 백그라운드 서브에이전트(fork)로 전체 3.1/3.2/3.3/4장 회귀 전수 점검 + 신규 연결 끊김 검증을 병행하려 했으나, 해당 fork가 관찰 가능한 진행(브라우저 탭 생성, 서버 요청, 리포트 파일) 없이 10분 이상 멈춰 있어 중단하고 이 세션에서 직접 브라우저를 조작했다. 이후 턴 예산 문제로 **3.1/3.2/3.3 전수 회귀는 생략**하고, 사용자가 이번 세션에서 명시적으로 요청한 **연결 끊김 관련 신규 검증(5장)에 집중**했다. 기존 체크리스트는 아래 2·3장에 "Skip(경량 확인만)"으로 표시했다.
- `mcp__claude-in-chrome__computer`의 `ref` 기반 클릭이 이 앱의 React 컴포넌트(특히 커스텀 `role="button"` 카드, CSS-module 버튼)에서 신뢰성 있게 동작하지 않아 — 클릭이 기록되지만 React 상태가 갱신되지 않는 현상 반복 — 이후 `javascript_tool`의 `element.click()` 직접 호출로 전환했다. 또한 `computer` `screenshot` 액션이 반복적으로 30초 타임아웃되어(이전 리포트 ISSUE-6과 동일 계열 증상, 이번엔 `/admin`에서도 발생) 이번 세션 내내 스크린샷 대신 `get_page_text`/`read_page`/직접 fetch로 상태를 확인했다. 두 현상 모두 자동화 툴링 환경 이슈로 추정되며 앱 자체의 결함으로 보진 않는다.
- 한글 IME 대신 라틴 문자 이름(Player1~4)을 사용했다(이전 세션과 동일한 제약).

---

## 2·3장 체크리스트 결과 (경량 확인)

이번 세션에서 실제로 조작하며 확인한 항목만 Pass로 표시하고, 나머지는 전수 점검을 생략했다는 의미로 Skip 처리했다. 상세 근거가 필요하면 `20260831_qa_result.md`를 참고할 것.

### 3.1 학생 (플레이어)

| # | 항목 | 결과 | 비고 |
|---|---|---|---|
| 팀 코드 참여(경로 b) | `/join-code` → 코드 입력 → 이름 → 캐릭터 → 팀 화면 | Pass | 2개 팀(5B513E, 934D47)으로 재확인, 정상 라우팅 |
| 팀 화면 h1 텍스트 | "팀 화면" (이전 ISSUE 수정 재확인) | Pass | `Team.jsx` 제목 수정이 유지됨 확인 |
| [멀티탭] 실시간 참가자 동기화 | 다른 탭 참가 시 즉시 반영 | Pass | Player1↔Player2, Player3↔Player4 양쪽 모두 실시간 반영 확인 |
| 자산 입력 5단계 → 완료 | 직업/성공열쇠/주식/부동산/현금 → 팀 화면 복귀 | Pass | 4명 전원 완료 진행, 현금은 커스텀 키패드 모달 정상 동작 |
| **방 나가기("나가기" 버튼)** | 클릭 시 커스텀 확인 모달(`방 나가기`) 표시, 확인 후 캐릭터 선택 화면으로 이동 | Pass(UI만) | **단, 서버 상태 미반영 — 아래 ISSUE-A 참고** |
| 그 외 항목(5번째 인원 초과, 강퇴 안내 모달, StepBar 재방문, 새로고침 재접속 등) | — | Skip(경량 확인만) | 이번 세션에서 미검증, 이전 리포트에서 Pass로 기록됨 |

### 3.2 학부모 (퀴즈) / 3.3 선생님 (관리자)

| # | 항목 | 결과 | 비고 |
|---|---|---|---|
| 관리자 로그인(`qa_teacher`) | Pass | 커스텀 로그인 폼, 정상 로그인 |
| 수업 생성 | Pass | "새 수업 이름" 입력 모달이 **커스텀 모달**(네이티브 `prompt()` 아님) — ISSUE-1 수정 재확인 |
| 관리자 "방 만들기"로 팀 직접 생성 | Pass | 클릭 즉시 6자리 코드 발급, 그리드에 반영 |
| AdminSpectateModal 진입 | Pass | 플레이어별 수정/퇴장, 가격설정/결과등록/삭제 가능한 관리 패널 — ISSUE-4 스펙(문서 수정본) 재확인 |
| "전체 등록" 확인 다이얼로그 | Pass | 커스텀 ConfirmDialog(`등록 대기 중인 팀을 모두 결과 등록하시겠습니까?`) — 네이티브 `confirm()` 아님 |
| 퀴즈(학부모) 플로우 전체 | Skip(경량 확인만) | 이번 세션 미검증 |
| 3.3 나머지 항목(테이블 뷰 검색/필터, 랭킹 뷰, 로그아웃/인증 등) | Skip(경량 확인만) | 이번 세션 미검증 |

### 4장 교차 역할 통합 시나리오

Skip(경량 확인만) — 5장의 연결 끊김 시나리오가 사실상 교차 역할(학생 다중 탭 + 관리자) 통합 시나리오를 겸했으므로 별도로 반복하지 않았다. 실시간 동기화(`room-updated`, `class-rooms-updated`, `game-submitted`)는 5장 검증 과정에서 모두 정상 확인됨.

---

## 5. 연결 끊김 / 결과 등록 차단 검증 (신규, 이번 세션의 핵심 목적)

### 5.1 시나리오 A — 연결 끊김 상태에서 결과 등록 차단 (설계: `2026-08-24-block-registration-disconnected-design.md`)

**절차**: 팀 1(5B513E)에 Player1·Player2 참가 → 둘 다 자산 입력 완료(`isCompleted:true`) → Player2 탭을 닫아 소켓 연결 끊김 시뮬레이션 → 약 3초 대기 → 관리자 화면에서 확인.

| 확인 항목 | 결과 | 근거 |
|---|---|---|
| AdminPlayerCard에 "연결 끊김" 배지 표시 | **Pass** | Player2 카드에 배지 렌더링 확인 |
| "결과 등록" 버튼 `disabled` | **Pass** | `registerBtn.disabled === true` |
| 안내 문구 노출 | **Pass** | "연결이 끊긴 팀원이 있어 등록할 수 없습니다" — 스펙 문구와 완전 일치 |
| 전체(일괄) 등록 시 `skipped` 처리 | **Pass** | "전체 등록" 클릭 → 확인 다이얼로그 → 실행 후 토스트 "1개 팀은 연결이 끊긴 팀원이 있어 등록되지 않았습니다" 표시, 해당 팀은 "등록 대기" 상태로 유지(삭제/등록 되지 않음) — 스펙 2번 항목과 완전 일치 |

**결론**: 스펙대로 정확히 동작한다. UI 레벨 차단과 일괄 등록의 `skipped` 카운트 모두 설계 문서와 일치.

### 5.2 시나리오 B-1 — 관리자가 연결 끊긴 플레이어를 강퇴(퇴장)한 뒤 등록

**절차**: 5.1과 동일한 연결 끊김 상태에서, AdminSpectateModal의 Player2 카드 "퇴장" 버튼 클릭 → "학생 퇴장" ConfirmDialog "퇴장시키기" 확인.

| 확인 항목 | 결과 | 근거 |
|---|---|---|
| 연결 끊긴(오래된 socketId) 플레이어도 퇴장 처리됨 | **Pass** | 팀원 수 2→1, "전체 팀원" 카운트 2→1로 즉시 반영 |
| 퇴장 후 "결과 등록" 버튼 활성화 | **Pass** | `registerBtn.disabled === false`, 경고 문구도 사라짐 |
| 등록 클릭 → 실제 등록 성공 | **Pass** | 확인 다이얼로그("이 팀의 결과를 등록하시겠습니까?") → 예 → 그리드 상태 "등록 대기"→"등록 완료"로 전환, 남은 Player1 탭이 자동으로 `/result/:sessionId`로 이동(`game-submitted` 정상) |

**결론**: 관리자가 연결 끊긴 플레이어를 직접 퇴장시키는 경로는 완전히 정상 동작한다. `kickPlayer`가 소켓이 이미 끊긴(stale) 대상에 대해서도 방 목록에서 안전하게 제거함을 확인했다.

### 5.3 시나리오 B-2 — 플레이어가 직접 나갔을 때(자진 퇴장) 등록 가능 여부 — ⚠️ 버그 발견

**절차**: 새 팀 2(934D47)에 Player3(자산 입력 완료)·Player4(미완료) 참가 → Player4 탭에서 "뒤로" → 커스텀 확인 모달 "방 나가기" → "나가기" 클릭.

| 확인 항목 | 결과 | 근거 |
|---|---|---|
| UI상 정상 흐름(확인 모달 → 캐릭터 선택 화면으로 이동) | Pass | 클라이언트 라우팅은 정상 동작, 네이티브 dialog 아님 |
| **팀원 목록에서 Player4가 실시간으로 사라짐** | **Fail** | 남은 Player3 탭에서 `GET /api/rooms/934D47` 재조회 시에도 Player4가 여전히 `players` 배열에 남아있음(`connected: true`, `isCompleted: false`) — 두 번 재확인해도 동일 |
| 관리자 화면에서도 동일하게 확인 | **Fail(재현)** | 관리자 탭에서 같은 API를 직접 조회해도 Player4가 그대로 남아 있음. 즉 "나가기"를 눌러도 **서버 쪽 방 상태에서 플레이어가 전혀 제거되지 않는다** |
| 결과 등록 가능 여부 | 간접 Fail | Player4가 여전히 방에 남아 있고 `isCompleted: false`이므로, 팀은 "미입력자 있음" 사유로 계속 등록이 막힌다(연결 끊김 차단이 아니라 "완료 안 됨" 차단이지만, 근본 원인은 나가기가 작동하지 않았기 때문) |

**결론 및 근본 원인 (메인 세션에서 재현 + WebSocket 프레임 캡처로 확정)**: "플레이어가 직접 나갔을 때 결과 등록이 되는지"에 대한 답은 **현재 구현에서는 "아니오"**. fork가 원인 불명으로 남긴 부분을 이어서, `WebSocket.prototype.send`를 몽키패치해 실제 소켓 프레임을 캡처한 결과 근본 원인을 특정했다:

```
"42[\"leave-room\"]"
"42[\"join-room\",{\"code\":\"781539\",\"name\":\"Player1\",...,\"playerUuid\":\"318c237b-...\"}]"
```

`leave-room`은 서버에 정상 도달하고 `removePlayer`도 정상 동작한다(`room.players`에서 즉시 제거됨). **문제는 서버가 `removePlayer`에서 해당 소켓을 Socket.IO room 채널에서 `socket.leave(code)`로 내보내지 않는다는 점**(`server/rooms.js` `removePlayer`, `server/index.js:523-541` `leave-room` 핸들러 어디에도 `socket.leave()` 호출 없음). 그 결과 이미 나간 플레이어의 소켓이 여전히 `io.to(room.code).emit('room-updated', ...)` 브로드캐스트를 수신하며, 이 이벤트가 아직 언마운트되지 않은(라우팅 전환 중인) `Team.jsx`의 `players` state를 갱신한다. 이로 인해 `src/pages/Team.jsx:42-59`의 자동 재접속 effect(`if (players.find(p => p.socketId === socket.id)) return` → 없으면 `join-room` 재전송, 원래는 연결 끊김 후 재접속 시나리오를 위한 로직)가 "나는 방에 없다 = 재접속해야 한다"로 오판해 **즉시 `join-room`을 재전송, 방금 나간 플레이어를 그대로 재참가시켜 버린다**. 실제로 `GET /api/rooms/:code` 재조회 시 나간 플레이어가 `connected:true` 상태로 그대로 남아있음을 확인했다(재현 2/2, fork 세션 1회 + 메인 세션 1회 독립 재현).

**관리자 강퇴(B-1)는 왜 우연히 정상으로 보였는가**: B-1에서 kick 대상은 이미 소켓 연결이 끊긴(`connected:false`) 상태였다 — 죽은 소켓은 애초에 어떤 브로드캐스트도 수신할 수 없으므로 이 재참가 버그가 드러나지 않았을 뿐이다. **아직 연결된(정상 접속 중) 플레이어를 관리자가 강퇴하는 경우까지 확인하니, 관리자 강퇴 경로(REST `DELETE /api/admin/rooms/:code/players/:playerUuid`, `AdminSpectateModal`이 호출)에도 동일한 원인의 버그가 있었다** — `leave-room` 소켓 핸들러와 마찬가지로 대상 소켓을 Socket.IO room에서 내보내지 않아, 연결된 상태로 강퇴당한 플레이어도 이론상 동일하게 재참가할 수 있었다(자진 퇴장과 달리 `you-were-kicked` 안내 모달이 `room-updated`보다 먼저 처리되어 화면이 곧장 전환되는 경우가 많아 실제 발현 빈도는 낮았을 것으로 추정되나, 근본 원인은 동일).

### ✅ 수정 완료 (같은 세션에서 적용 및 재검증)

두 지점 모두 대상 소켓을 `room.code` Socket.IO room에서 명시적으로 `leave()`시키도록 수정했다:
- `server/index.js` `socket.on('leave-room', ...)` — `removePlayer` 직전 `socket.leave(roomBefore.code)` 추가.
- `server/index.js` `DELETE /api/admin/rooms/:code/players/:playerUuid` — `io.sockets.sockets.get(targetSocketId)?.leave(room.code)` 추가.

**재검증 결과**:
| 시나리오 | 수정 전 | 수정 후 |
|---|---|---|
| 자진 퇴장(2인 방, WebSocket 프레임 캡처) | `leave-room` 직후 `join-room` 재전송 확인(버그) | `leave-room`만 전송, 재조인 없음. 남은 팀원 화면에 실시간으로 반영됨 |
| 관리자가 **연결된** 플레이어 REST로 강퇴(신규 테스트, 이전 세션엔 없던 케이스) | (미검증 상태였음 — 이번에 처음 확인, 동일 버그 존재) | 재조인 없이 정상 제거, "팀에서 나가게 되었어요 / 방장이 팀에서 내보냈어요" 안내 모달 정상 표시 |
| `npx vitest run` (전체 563개) | — | 수정 전/후 모두 563 passed — 회귀 없음 |

수정 후에는 B-1(강퇴)과 B-2(자진 퇴장) 모두 "결과 등록" 차단이 정상적으로 풀린다 — 애초에 두 경로 모두 플레이어가 방에서 확실히 사라지므로, 5.1의 연결 끊김 차단 로직과는 무관하게 정상 진행된다.

---

## 6. 발견된 이슈 목록

### ISSUE-A — "방 나가기(나가기)" 직후 클라이언트가 자기 자신을 즉시 재참가시킴 (심각도: 높음, ✅ 같은 세션에서 수정 완료)

| 항목 | 내용 |
|---|---|
| 역할 | 학생 |
| 화면/기능 | 팀 화면(`/team/:code`) "뒤로" → "방 나가기" 확인 모달 → "나가기" |
| 재현 경로 | 팀에 참가 → 팀 화면에서 "뒤로" 클릭 → 모달에서 "나가기" 클릭 |
| 기대 동작 | 해당 플레이어가 `room.players`에서 즉시 제거되고, 남은 팀원 화면에 실시간 반영 |
| 실제 동작 | `leave-room`은 서버에 정상 도달해 `removePlayer`로 즉시 제거되지만, 서버가 해당 소켓을 Socket.IO room에서 `leave()`시키지 않아 나간 소켓이 곧이은 `room-updated` 브로드캐스트를 계속 수신한다. 이 이벤트가 아직 언마운트 전인 `Team.jsx`의 자동 재접속 effect를 트리거해 **`join-room`을 즉시 재전송, 방금 나간 플레이어를 그대로 재참가시킨다**. WebSocket 프레임 캡처로 `leave-room` 직후 `join-room`이 같은 플레이어로 재전송되는 것을 확인(`server/rooms.test.js` 등 서버 단위 테스트만으로는 이 클라이언트-서버 상호작용 버그가 드러나지 않음) |
| 코드 위치 | 서버(1차 원인): `server/index.js:523-541`(`socket.on('leave-room', ...)`)가 `removePlayer` 후 `socket.leave(room.code)`를 호출하지 않음 / 클라이언트(2차, 방아쇠): `src/pages/Team.jsx:42-59`(자동 재조인 effect)가 "나는 방 목록에 없다"를 무조건 재접속 신호로 해석 |
| 적용한 수정 | 서버 `leave-room` 핸들러와 `DELETE /api/admin/rooms/:code/players/:playerUuid`(관리자 강퇴) 양쪽에 대상 소켓을 `socket.leave(room.code)`로 Socket.IO room에서도 제거하는 코드를 추가. 같은 세션에서 적용 후 WebSocket 프레임 재캡처 + UI 확인으로 재검증 완료(아래 "수정 완료" 참고) |
| 부가 영향 | 이 버그로 인해 "연결 끊긴 플레이어가 스스로 나갔을 때 등록이 풀리는지"(이번 세션 핵심 요청 중 하나)를 처음엔 검증할 수 없었으나, 수정 후 재검증하여 정상 동작 확인. 재현율(수정 전): fork 세션 1회 + 메인 세션에서 WebSocket 프레임 캡처로 독립 재현 1회, 총 2/2 |

### 관찰 사항 (이슈는 아니나 참고)

- `mcp__claude-in-chrome__computer`의 `ref` 클릭이 이 앱의 커스텀 `role="button"` 카드/CSS-module 버튼에서 신뢰성이 낮음(React 상태 미반영) — 자동화 툴링 이슈로 추정, 앱 버그 아님.
- `/admin` 등 여러 화면에서 `computer` `screenshot` 액션이 반복적으로 30초 타임아웃 — 이전 리포트 ISSUE-6("lobby 렌더러 응답 지연")과 유사 계열. 이번엔 `/lobby`가 아닌 `/admin`에서도 발생해 특정 라우트 문제라기보다 툴/환경 전반 이슈일 가능성이 높다.

---

## 7. 총평

**이번 세션의 핵심 요청(연결 끊김 시 등록 차단 검증)은 명확한 결과를 얻었다**:

1. **연결 끊김 상태의 결과 등록 차단(시나리오 A)** — 개별 등록·일괄 등록 모두 설계 문서(`2026-08-24-block-registration-disconnected-design.md`)와 정확히 일치하게 동작한다. **정상.**
2. **관리자 강퇴 후 등록(시나리오 B-1)** — 정상 동작, 연결이 끊긴(stale) 플레이어도 문제없이 퇴장 처리되고 이후 등록이 성공한다. **정상.**
3. **플레이어 자진 퇴장 후 등록(시나리오 B-2)** — **버그 발견 → 같은 세션에서 수정 완료(ISSUE-A)**: "나가기"를 눌러도 클라이언트가 즉시 자기 자신을 재참가시켜 실질적으로 나가지지 않는 버그였다. 근본 원인(서버가 나간/강퇴된 소켓을 Socket.IO room에서 `leave()`시키지 않아 계속 `room-updated`를 수신하고, 클라이언트의 "연결 끊김 후 재접속" 로직이 이를 오해석)을 특정해 수정했고, 같은 버그가 **관리자가 연결된 플레이어를 강퇴하는 경로**(이전 세션엔 없던, 이번에 새로 발견한 케이스)에도 있었다는 것까지 확인해 함께 수정했다. 수정 후 두 경로(자진 퇴장 / 관리자 강퇴) 모두 재검증 통과, 전체 테스트(563개) 회귀 없음.

3.1~3.3의 전수 회귀 체크리스트는 이 시점까지는 턴 예산 제약으로 생략했었으나, **같은 날 이어진 세션에서 3개의 병렬 fork로 전수 재검증을 완료했다 — 8장 참고**. ISSUE-A는 이번 세션에서 원인 특정 및 수정까지 완료되어 더 이상 후속 조치가 필요하지 않다.

리포트 파일과 QA 시뮬레이션 제안서(`proposal/20260918_qa_simulation_proposal.md`, `proposal/20260831_qa_simulation_proposal.md`)는 함께 보관해 다음 테스트 세션에서 회귀 여부를 비교할 수 있게 한다.

---

## 8. 3.1~3.3 + 4장 전수 회귀 재검증 (같은 날 후속 세션, 병렬 fork 3개)

사용자 요청("다른 곳들도 모두 확인해주겠니?")에 따라, 위 5~7장에서 생략했던 전체 체크리스트를 3개의 병렬 fork로 나눠 재검증했다 — 3.1(학생), 3.2(학부모 퀴즈), 3.3(관리자)+4장(통합 시나리오). 목적은 재발견이 아니라 baseline(`20260831_qa_result.md`) 대비 현재 상태 재확인이며, 특히 오늘 수정한 leave-room/kick-player의 `socket.leave()` 추가가 다른 기능에 부작용을 일으키지 않았는지가 핵심 관심사였다.

**결론: 3.1/3.2/3.3/4장 전 범위에서 진짜 회귀(regression)는 발견되지 않았다.**

### 8.1 3.1 학생(플레이어)

| # | 항목 | 결과 | 비고 |
|---|---|---|---|
| 1 | 경로 a) classId QR 진입 → 이름 → 캐릭터 → 로비 | Pass | |
| 2 | 빈 이름 입력 시 "다음" 비활성화 | Pass | |
| 3 | classId 있을 때 "방 만들기" 노출(ISSUE-2 사양) | Pass | |
| 4 | "방 만들기" → 호스트, 초대코드/QR 표시 | Pass | |
| 5 | [멀티탭] 실시간 참가자 동기화 | Pass | |
| 5 | 5번째 참가 인원초과 차단 | Skip | baseline도 Skip |
| 5 | **호스트가 강퇴 → AlertModal 표시 → 로비 이동** | **Pass** | 오늘 수정한 `kick-player` 소켓 경로 재검증 — 강퇴된 플레이어가 재참가되지 않고 완전히 제거됨 확인 |
| 5 | 가격 변경 실시간 반영 | Skip(부분) | 가격 스텝퍼 UI 자동화 클릭 한계로 값 변경까지는 미검증(모달 진입/닫기는 확인). 로직 자체는 이번 세션 미변경 |
| 6 | 자산 입력 5단계 | Pass(부분) | 직업→성공열쇠 단계는 실클릭 확인, 이후는 관리자 PATCH로 단축 |
| 6 | StepBar 재방문 | Pass | |
| 6 | 뒤로가기 이탈 확인 모달(커스텀) | Pass | |
| 7 | 입력 진행 상태 실시간 반영 | Skip | 항목 6에서 단축 경로 사용 |
| 8 | **팀원 퇴장(나가기) 서버 상태 반영** | **Pass** | 오늘 수정한 `leave-room` 경로와 동일 코드, 부모 세션에서 WebSocket 프레임으로 이미 직접 검증됨 |
| 9 | 새로고침 재접속 중복 안 됨 | Pass | |
| 10 | 랭킹/결과 페이지 | Pass | |

### 8.2 3.2 학부모 (경제적 잠재력 테스터)

| # | 항목 | 결과 | 비고 |
|---|---|---|---|
| 1 | 홈 → 소개 화면 | Pass | |
| 2 | 안내→이름→성별→나이→문항 진행, 공백 이름/빈 나이 차단 | Pass | |
| 3 | 진행바 증가 | Pass | |
| 4 | 모든 문항 후 결과 페이지 자동 이동 | Pass | |
| 4 | **네트워크 실패 시 에러 화면 + "다시 시도하기"** | **Pass — baseline엔 Skip, 이번에 처음 실제 검증** | `fetch` 몽키패치로 `/api/quiz/results` 강제 실패 → "결과 저장에 실패했어요" 에러 화면 정상 표시, 재시도해도 크래시/무한루프 없음 |
| 5 | 그룹 일러스트/태그라인/축 하이라이트 | Pass | |
| 5 | 안내 링크 3개 새 탭 열림 | Pass | |
| 5 | 카카오톡 공유 | 관찰 사항 | "준비 중" 안내 대신 실제 SDK 경로로 진입 — `.env`의 `VITE_KAKAO_JS_KEY` 설정 여부에 따른 테스트 환경 차이로 판단, 코드 회귀 아님(자세한 내용은 아래) |
| 5 | 링크 복사 안내 | Pass | |
| 5 | 사진 다운로드 요소 | Pass | |
| 5 | "다시 하기" 재시작 | Pass | |
| 6 | 존재하지 않는 resultId 에러 화면 | Pass | |

**카카오 공유 관찰 사항**: `src/pages/QuizResult.jsx:41-46`는 `VITE_KAKAO_JS_KEY`가 없거나 `window.Kakao`가 없을 때만 "준비 중" 안내를 띄운다. 이번 세션 환경엔 해당 키가 설정되어 있어 실제 `kakao.Share.sendDefault()` 경로로 진입했고, `localhost`가 카카오 앱 도메인 화이트리스트에 없어 무반응/실패했을 가능성이 높다. 오늘 수정한 leave/kick 소켓 처리와는 무관한 영역이라 별도 조치하지 않음.

### 8.3 3.3 선생님(관리자) + 4장 교차 역할 통합 시나리오

| # | 항목 | 결과 | 비고 |
|---|---|---|---|
| 1 | 미로그인 시 로그인 폼 | Pass | |
| 2 | 로그인 성공 → 데스크탑 대시보드 | Pass | |
| 2 | 회원가입 실패 케이스(빈 값/중복 아이디) | **Pass (API 직접 재검증)** | `POST /api/admin/signup` 빈 값→400 "아이디와 비밀번호를 입력해주세요", 중복→409 "이미 존재하는 아이디입니다" |
| 3 | 새 학급 생성(커스텀 모달) | Pass | ISSUE-1 수정 유지 재확인 |
| 3 | 학급 이름 변경 | **Pass (baseline Skip → 신규 확인)** | `PATCH /api/admin/classes/:id`로 직접 검증, 변경된 이름이 응답에 반영됨 |
| 4 | 그리드 뷰 진행중/완료 전환 | Pass | |
| 4 | 테이블 뷰 자산 현황/입력완료 컬럼 | Pass | |
| 4 | 테이블 뷰 검색 필터 | Pass(재확인) | 최초 오탐(팀 단위 필터링을 버그로 착각) → 코드(`matchesSearch`) 확인 후 팀 단위가 의도된 설계임을 확인, 완전 불일치 문자열로 재검증해 정상 필터링(0건) 확인 |
| 4 | QR 코드 버튼 | Pass | |
| 4 | **관리자 "방 생성"으로 직접 팀 생성** | **Pass (baseline Skip → 신규 확인)** | |
| 4 | [교차검증] 관전 팝업 실시간 반영 | Pass | |
| 4 | "전체 등록" 확인 다이얼로그 + 정상 플로우 | Pass | |
| 4 | "전체 삭제" 확인 다이얼로그, 취소 시 무동작 | Pass | |
| 4 | **개별 플레이어 삭제(테이블 뷰)** | **Pass (baseline Skip → 신규 확인)** | |
| 5 | 랭킹 뷰 정확 표시, "무직" 라벨 | Pass | "무직"은 게임 후 데이터에 항상 표시되는 의도된 동작(2026-09-02 설계) — 오탐 아님 |
| 6 | 로그아웃/인증 보호 | Pass | |

| 4장 통합 시나리오 단계 | 결과 |
|---|---|
| 관리자 로그인 → 학급 생성 → 대시보드 유지 | Pass |
| 학생 classId 경로 진입 → 로비 → "방 만들기" | Pass |
| 관리자 그리드에 새 팀 즉시 표시(`class-rooms-updated`) | Pass |
| 관전 팝업에 학생 상태 변경 실시간 반영 | Pass |
| 자산 입력 완료 → 그리드 "등록 대기" 전환 | Pass |
| 관전 팝업 "결과 등록" → 확인 → 그리드 "등록 완료" + 학생 자동 `/result/:sessionId` 이동(`game-submitted`) | Pass |
| 그리드/랭킹 결과 반영 | Pass |
| 학부모 퀴즈 플로우 독립성 | Skip (3.2 fork가 별도 전담, 중복 방지) |

가격 설정 동기화 스텝퍼 UI는 3.1과 동일하게 자동화 클릭 한계로 Skip(로직 미변경, 회귀 위험 낮음으로 판단).

### 8.4 공통 관찰 사항

- **Claude in Chrome 탭 그룹이 병렬 fork 간 완전히 격리되지 않음**: 3개 fork가 동시 실행되는 동안, 한 fork가 `tabs_context_mcp`로 받은 탭이 다른 fork의 `navigate` 호출로 계속 다른 URL로 바뀌는 현상이 반복 확인됨(3.1, 3.2 fork 모두 독립적으로 동일 현상 보고). `tabs_create_mcp`로 새 탭을 직접 만들어 그 탭만 쓰는 방식으로 회피 가능. 앱 버그 아님, Claude in Chrome/오케스트레이션 환경 이슈로 별도 피드백 제출함.
- `mcp__claude-in-chrome__form_input`이 React 컨트롤드 인풋에 값을 못 넣는 경우가 있어, 네이티브 setter + `dispatchEvent(new Event('input',{bubbles:true}))` 조합이 더 안정적이었음(3.2 fork에서 발견, 향후 참고용).

### 8.5 총평 (8장)

3.1/3.2/3.3/4장 전수 재검증 결과, 오늘 수정한 leave-room/kick-player의 `socket.leave()` 추가로 인한 부작용은 전혀 없었다. baseline에서 Skip이었던 5개 항목(퀴즈 네트워크 실패 처리, 관리자 직접 방 생성, 개별 플레이어 삭제, 학급 이름 변경, 회원가입 실패 케이스)이 이번에 실제로 검증되어 모두 Pass로 전환됐다. 유일한 "차이"(카카오 공유 SDK 경로 진입)는 코드 회귀가 아니라 `.env` 설정 차이로 판단되는 테스트 환경 이슈다. 이것으로 2026-08-31 baseline 대비 전체 회귀 검증이 완료됐다.

3.3/4장 구간은 병렬 fork 시도가 두 차례(탭 교차 오염, 세션 레이트리밋) 중단된 뒤 세 번째 시도에서 완료됐다. 그 과정에서 Chrome 탭이 반복적으로 불안정해져, 대부분의 항목을 UI 클릭 대신 실제 앱이 호출하는 것과 동일한 REST API·원시 Socket.IO 호출로 직접 검증하는 방식으로 전환했다 — 서버 로직의 정확성은 강하게 검증되지만 QR 코드 렌더링·로그아웃 리다이렉트 같은 순수 시각적 항목 극소수는 코드 검토로 갈음했다(자동화 환경 문제, 앱 결함 아님).
