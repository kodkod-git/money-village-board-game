# 2026-06-29 변경사항 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 뒤로가기 버튼 통합, 방장 추방 기능, 랭킹 페이지 개선 3건, 개인 페이지 개선 3건을 구현한다.

**Architecture:** 공용 `BackButton` 컴포넌트를 먼저 만들어 7개 페이지에 적용한다. 추방 기능은 서버에 `kickPlayer` 함수를 추가하고 소켓 이벤트로 연결한다. 개인 페이지의 순차 팝업은 `isSequential` 플래그와 `POPUP_SEQUENCE` 배열로 흐름을 제어한다.

**Tech Stack:** React 18, React Router v6, Socket.IO, Vitest, @testing-library/react

---

## 파일 변경 요약

| 파일 | 유형 |
|------|------|
| `src/components/BackButton.jsx` | 신규 |
| `src/components/BackButton.module.css` | 신규 |
| `src/components/BackButton.test.jsx` | 신규 |
| `src/components/PlayerSlot.jsx` | 수정 |
| `src/components/PlayerSlot.module.css` | 수정 |
| `src/components/PlayerSlot.test.jsx` | 수정 |
| `src/components/RankingTable.jsx` | 수정 |
| `src/components/RankingTable.test.jsx` | 수정 |
| `src/pages/NameInput.jsx` | 수정 |
| `src/pages/CharacterSelect.jsx` | 수정 |
| `src/pages/Home.jsx` | 수정 |
| `src/pages/Lobby.jsx` | 수정 |
| `src/pages/IndividualPage.jsx` | 수정 |
| `src/pages/IndividualPage.module.css` | 수정 |
| `src/pages/RankingPage.jsx` | 수정 |
| `src/pages/ResultPage.jsx` | 수정 |
| `server/rooms.js` | 수정 |
| `server/index.js` | 수정 |

---

## Task 1: BackButton 컴포넌트 생성

**Files:**
- Create: `src/components/BackButton.jsx`
- Create: `src/components/BackButton.module.css`
- Create: `src/components/BackButton.test.jsx`

- [ ] **Step 1: 실패하는 테스트 작성**

`src/components/BackButton.test.jsx`:
```jsx
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { MemoryRouter } from 'react-router-dom'
import { describe, it, expect, vi } from 'vitest'

const mockNavigate = vi.fn()
vi.mock('react-router-dom', async () => {
  const actual = await vi.importActual('react-router-dom')
  return { ...actual, useNavigate: () => mockNavigate }
})

import BackButton from './BackButton'

describe('BackButton', () => {
  it('<를 렌더링한다', () => {
    render(<MemoryRouter><BackButton /></MemoryRouter>)
    expect(screen.getByRole('button', { name: '뒤로 가기' })).toBeInTheDocument()
  })

  it('클릭 시 navigate(-1)을 호출한다', async () => {
    render(<MemoryRouter><BackButton /></MemoryRouter>)
    await userEvent.click(screen.getByRole('button', { name: '뒤로 가기' }))
    expect(mockNavigate).toHaveBeenCalledWith(-1)
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
npx vitest run src/components/BackButton.test.jsx
```
Expected: FAIL — `Cannot find module './BackButton'`

- [ ] **Step 3: BackButton 컴포넌트 구현**

`src/components/BackButton.jsx`:
```jsx
import { useNavigate } from 'react-router-dom'
import styles from './BackButton.module.css'

export default function BackButton() {
  const navigate = useNavigate()
  return (
    <button className={styles.btn} onClick={() => navigate(-1)} aria-label="뒤로 가기">
      &lt;
    </button>
  )
}
```

`src/components/BackButton.module.css`:
```css
.btn {
  position: fixed;
  top: 16px;
  left: 16px;
  z-index: 100;
  width: 48px;
  height: 48px;
  border-radius: 50%;
  border: 2px solid rgba(255, 255, 255, 0.6);
  background: rgba(0, 0, 0, 0.25);
  color: #fff;
  font-size: 22px;
  font-weight: 700;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  backdrop-filter: blur(4px);
  line-height: 1;
}
.btn:hover {
  background: rgba(0, 0, 0, 0.45);
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
npx vitest run src/components/BackButton.test.jsx
```
Expected: PASS (2 tests)

- [ ] **Step 5: 커밋**

```bash
git add src/components/BackButton.jsx src/components/BackButton.module.css src/components/BackButton.test.jsx
git commit -m "feat: add BackButton component"
```

---

## Task 2: BackButton 전체 페이지 적용

**Files:**
- Modify: `src/pages/NameInput.jsx`
- Modify: `src/pages/CharacterSelect.jsx`
- Modify: `src/pages/Home.jsx`
- Modify: `src/pages/Lobby.jsx`
- Modify: `src/pages/IndividualPage.jsx`
- Modify: `src/pages/RankingPage.jsx`
- Modify: `src/pages/ResultPage.jsx`

- [ ] **Step 1: NameInput에 BackButton 추가**

`src/pages/NameInput.jsx` — import 추가 후 `<div className={styles.page}>` 바로 아래에 삽입:
```jsx
import BackButton from '../components/BackButton'
// ...
return (
  <div className={styles.page}>
    <BackButton />
    <div className={styles.card}>
```

- [ ] **Step 2: CharacterSelect에 BackButton 추가**

`src/pages/CharacterSelect.jsx`:
```jsx
import BackButton from '../components/BackButton'
// ...
return (
  <div className={styles.page}>
    <BackButton />
    <div className={styles.header}>
```

- [ ] **Step 3: Home에 BackButton 추가**

`src/pages/Home.jsx`:
```jsx
import BackButton from '../components/BackButton'
// ...
return (
  <div className={styles.page}>
    <BackButton />
    <h1 className={styles.title}>
```

- [ ] **Step 4: Lobby에 BackButton 추가**

`src/pages/Lobby.jsx`:
```jsx
import BackButton from '../components/BackButton'
// ...
return (
  <div className={styles.page}>
    <BackButton />
    <div className={styles.topBar}>
```

- [ ] **Step 5: IndividualPage에 BackButton 추가 및 기존 버튼 제거**

`src/pages/IndividualPage.jsx`:
```jsx
import BackButton from '../components/BackButton'
```

`export default function IndividualPage()` 의 return 안에서:
- `<button className={styles.backBtn} onClick={() => navigate(`/lobby/${code}`)}>← 뒤로</button>` 줄을 삭제
- `<div className={styles.page}>` 바로 아래에 `<BackButton />` 추가:

```jsx
return (
  <div className={styles.page}>
    <BackButton />
    <div className={styles.main}>
```

- [ ] **Step 6: RankingPage에 BackButton 추가 및 기존 버튼 제거**

`src/pages/RankingPage.jsx`:
```jsx
import BackButton from '../components/BackButton'
```

header 섹션에서 `<button className={styles.backBtn} onClick={() => navigate('/')}>← 홈</button>` 줄을 삭제하고, `<div className={styles.page}>` 바로 아래에 `<BackButton />` 추가:

```jsx
return (
  <div className={styles.page}>
    <BackButton />
    <div className={styles.inner}>
      <div className={styles.header}>
        <h1 className={styles.title}>🏆 랭킹</h1>
      </div>
```

- [ ] **Step 7: ResultPage에 BackButton 추가 및 기존 버튼 제거**

`src/pages/ResultPage.jsx`:
```jsx
import BackButton from '../components/BackButton'
```

`<button className={styles.backBtn} onClick={() => navigate(-1)}>← 뒤로</button>` 줄을 삭제하고, `<div className={styles.page}>` 바로 아래에 `<BackButton />` 추가:

```jsx
return (
  <div className={styles.page}>
    <BackButton />
    <div className={styles.wrapper}>
```

- [ ] **Step 8: 전체 테스트 실행 후 커밋**

```bash
npm test -- --run
```
Expected: 기존 테스트 모두 PASS

```bash
git add src/pages/NameInput.jsx src/pages/CharacterSelect.jsx src/pages/Home.jsx src/pages/Lobby.jsx src/pages/IndividualPage.jsx src/pages/RankingPage.jsx src/pages/ResultPage.jsx
git commit -m "feat: apply BackButton to all pages, remove old back buttons"
```

---

## Task 3: 추방 기능 — 서버

**Files:**
- Modify: `server/rooms.js`
- Modify: `server/index.js`

- [ ] **Step 1: rooms.js에 kickPlayer 함수 추가**

`server/rooms.js`의 `export function updateRoomPrices` 위에 추가:

```js
export function kickPlayer(hostSocketId, targetPlayerUuid) {
  const code = socketToRoom.get(hostSocketId)
  if (!code) return null
  const room = rooms.get(code)
  if (!room) return null
  const host = room.players.find(p => p.socketId === hostSocketId)
  if (!host?.isHost) return null
  const target = room.players.find(p => p.playerUuid === targetPlayerUuid)
  if (!target) return null
  room.players = room.players.filter(p => p.playerUuid !== targetPlayerUuid)
  socketToRoom.delete(target.socketId)
  return { room, targetSocketId: target.socketId }
}
```

- [ ] **Step 2: server/index.js에 kick-player 이벤트 핸들러 추가**

`server/index.js`의 import 줄에 `kickPlayer` 추가:
```js
import { createRoom, getRoom, addPlayer, removePlayer, updatePlayerState, updateRoomPrices, kickPlayer } from './rooms.js'
```

`io.on('connection', ...)` 블록 안, `socket.on('leave-room', ...)` 위에 추가:
```js
socket.on('kick-player', ({ playerUuid }) => {
  const result = kickPlayer(socket.id, playerUuid)
  if (!result) return
  const { room, targetSocketId } = result
  io.to(targetSocketId).emit('you-were-kicked')
  io.to(room.code).emit('room-updated', { players: room.players })
})
```

- [ ] **Step 3: 서버 수동 테스트**

`npm run dev` 실행 후:
1. 두 브라우저에서 같은 방에 접속
2. 방장 브라우저에서 X 버튼이 없는 것 확인 (클라이언트는 다음 Task에서 추가)

- [ ] **Step 4: 커밋**

```bash
git add server/rooms.js server/index.js
git commit -m "feat: add kickPlayer server function and kick-player socket event"
```

---

## Task 4: 추방 기능 — 클라이언트

**Files:**
- Modify: `src/components/PlayerSlot.jsx`
- Modify: `src/components/PlayerSlot.module.css`
- Modify: `src/components/PlayerSlot.test.jsx`
- Modify: `src/pages/Lobby.jsx`
- Modify: `src/pages/IndividualPage.jsx`

- [ ] **Step 1: PlayerSlot 테스트에 onKick 케이스 추가**

`src/components/PlayerSlot.test.jsx`에 추가:
```jsx
import userEvent from '@testing-library/user-event'

it('onKick이 전달되면 이름 옆에 X 버튼을 표시한다', () => {
  const onKick = vi.fn()
  render(<PlayerSlot player={{ name: '철수', character: 'ptsc', isHost: false }} onKick={onKick} />)
  expect(screen.getByRole('button', { name: '추방' })).toBeInTheDocument()
})

it('onKick이 없으면 X 버튼을 표시하지 않는다', () => {
  render(<PlayerSlot player={{ name: '철수', character: 'ptsc', isHost: false }} />)
  expect(screen.queryByRole('button', { name: '추방' })).toBeNull()
})

it('X 버튼 클릭 시 onKick을 호출한다', async () => {
  const onKick = vi.fn()
  render(<PlayerSlot player={{ name: '철수', character: 'ptsc', isHost: false }} onKick={onKick} />)
  await userEvent.click(screen.getByRole('button', { name: '추방' }))
  expect(onKick).toHaveBeenCalledOnce()
})
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
npx vitest run src/components/PlayerSlot.test.jsx
```
Expected: 신규 3개 FAIL

- [ ] **Step 3: PlayerSlot.jsx에 onKick prop 추가**

`src/components/PlayerSlot.jsx` 전체 교체:
```jsx
import styles from './PlayerSlot.module.css'

export default function PlayerSlot({ player, isOwnPlayer, onNavigate, onKick }) {
  if (!player) {
    return (
      <div className={styles.slot}>
        <div className={styles.emptyImg} />
        <div className={styles.badgeEmpty}>대기 중...</div>
      </div>
    )
  }
  return (
    <div
      className={`${styles.slot} ${isOwnPlayer ? styles.clickable : ''}`}
      onClick={isOwnPlayer ? onNavigate : undefined}
    >
      <img
        src={`/characters/${player.character}.png`}
        alt={player.character}
        className={styles.img}
      />
      <div className={styles.badge}>
        <span className={styles.name}>{player.name}</span>
        {player.isHost && <span className={styles.host}>방장 ★</span>}
        {player.gameState?.isCompleted && <span className={styles.completed}>입력완료</span>}
        {onKick && (
          <button
            className={styles.kickBtn}
            onClick={e => { e.stopPropagation(); onKick() }}
            aria-label="추방"
          >
            ✕
          </button>
        )}
      </div>
    </div>
  )
}
```

- [ ] **Step 4: PlayerSlot.module.css에 kickBtn 스타일 추가**

`src/components/PlayerSlot.module.css` 파일 끝에 추가:
```css
.kickBtn {
  display: inline-block;
  margin-left: 6px;
  padding: 2px 7px;
  border: none;
  border-radius: 6px;
  background: #e53935;
  color: #fff;
  font-size: clamp(7px, 1.4vw, 14px);
  font-weight: 700;
  cursor: pointer;
  line-height: 1.4;
  vertical-align: middle;
}
.kickBtn:hover {
  background: #c62828;
}
```

- [ ] **Step 5: PlayerSlot 테스트 통과 확인**

```bash
npx vitest run src/components/PlayerSlot.test.jsx
```
Expected: PASS (전체)

- [ ] **Step 6: Lobby.jsx에 kick 로직 추가**

`src/pages/Lobby.jsx`의 `slots.map(...)` 부분을 수정:
```jsx
{slots.map((player, i) => (
  <PlayerSlot
    key={i}
    player={player}
    isOwnPlayer={player?.socketId === socket?.id}
    onNavigate={() => navigate(`/lobby/${code}/individual`)}
    onKick={
      isHost && player && player.socketId !== socket?.id
        ? () => socket?.emit('kick-player', { playerUuid: player.playerUuid })
        : undefined
    }
  />
))}
```

Lobby의 기존 useEffect 블록들 아래에 `you-were-kicked` 핸들러 추가:
```jsx
useEffect(() => {
  if (!socket) return
  const handler = () => navigate('/team')
  socket.on('you-were-kicked', handler)
  return () => socket.off('you-were-kicked', handler)
}, [socket, navigate])
```

- [ ] **Step 7: IndividualPage.jsx에 you-were-kicked 핸들러 추가**

`src/pages/IndividualPage.jsx`의 기존 useEffect들 아래에 추가:
```jsx
useEffect(() => {
  if (!socket) return
  const handler = () => navigate('/team')
  socket.on('you-were-kicked', handler)
  return () => socket.off('you-were-kicked', handler)
}, [socket, navigate])
```

- [ ] **Step 8: 전체 테스트 후 커밋**

```bash
npm test -- --run
```
Expected: PASS

```bash
git add src/components/PlayerSlot.jsx src/components/PlayerSlot.module.css src/components/PlayerSlot.test.jsx src/pages/Lobby.jsx src/pages/IndividualPage.jsx
git commit -m "feat: implement kick player feature"
```

---

## Task 5: 랭킹 탭명 변경 + key 중복 버그 수정

**Files:**
- Modify: `src/pages/RankingPage.jsx`
- Modify: `src/components/RankingTable.jsx`
- Modify: `src/components/RankingTable.test.jsx`

- [ ] **Step 1: key 중복 버그 테스트 추가**

`src/components/RankingTable.test.jsx`에 추가:
```jsx
it('같은 playerUuid가 여러 행에 있을 때 각 행을 독립적으로 렌더링한다', () => {
  const duplicateRows = [
    { rank: 1, name: '홍길동', affiliation: '경영학과', character: 'fox', totalAssets: 200000, playerUuid: 'uuid-1', sessionId: 'session-A' },
    { rank: 2, name: '홍길동', affiliation: '경영학과', character: 'fox', totalAssets: 150000, playerUuid: 'uuid-1', sessionId: 'session-B' },
  ]
  render(<MemoryRouter><RankingTable rows={duplicateRows} /></MemoryRouter>)
  const rows = screen.getAllByText('홍길동')
  expect(rows).toHaveLength(2)
})
```

- [ ] **Step 2: 테스트 실행 (현재 상태 확인)**

```bash
npx vitest run src/components/RankingTable.test.jsx
```
Expected: 신규 테스트 통과하거나 실패 (key 중복 경고는 console에 표시됨)

- [ ] **Step 3: RankingTable.jsx key 수정**

`src/components/RankingTable.jsx`의 tbody rows 부분에서 key를 수정:
```jsx
{rows.map(row => (
  <tr
    key={`${row.sessionId ?? ''}-${row.playerUuid ?? `${row.rank}-${row.name}`}`}
    className={styles.tr}
    onClick={onRowClick ? () => onRowClick(row) : undefined}
    style={onRowClick ? { cursor: 'pointer' } : undefined}
  >
```

- [ ] **Step 4: RankingPage.jsx 탭명 변경**

`src/pages/RankingPage.jsx`의 `TABS` 배열을 수정:
```js
const TABS = [
  { key: 'global', label: '전체' },
  { key: 'affiliation', label: '소속' },
  { key: 'team', label: '팀' },
]
```

- [ ] **Step 5: 테스트 통과 확인 후 커밋**

```bash
npx vitest run src/components/RankingTable.test.jsx
```
Expected: PASS

```bash
git add src/pages/RankingPage.jsx src/components/RankingTable.jsx src/components/RankingTable.test.jsx
git commit -m "fix: fix duplicate key bug in RankingTable, rename ranking tabs"
```

---

## Task 6: 랭킹 플레이스홀더 행

**Files:**
- Modify: `src/components/RankingTable.jsx`
- Modify: `src/components/RankingTable.module.css` (스타일 추가)
- Modify: `src/components/RankingTable.test.jsx`
- Modify: `src/pages/RankingPage.jsx`

- [ ] **Step 1: 플레이스홀더 테스트 추가**

`src/components/RankingTable.test.jsx`에 추가:
```jsx
it('highlightPlayerUuid가 있지만 rows에서 찾을 수 없을 때 플레이스홀더 행을 렌더링한다', () => {
  render(
    <MemoryRouter>
      <RankingTable rows={mockRows} highlightPlayerUuid="uuid-unknown" />
    </MemoryRouter>
  )
  const empty = screen.getByTestId('pinned-row-empty')
  expect(empty).toBeInTheDocument()
  expect(empty).toHaveTextContent('게임에 참여하러 가기')
  expect(empty).toHaveTextContent('-위')
  expect(empty).toHaveTextContent('-원')
})

it('플레이스홀더 행 클릭 시 onRowClick에 isPlaceholder: true 객체를 전달한다', async () => {
  const handleClick = vi.fn()
  render(
    <MemoryRouter>
      <RankingTable rows={mockRows} highlightPlayerUuid="uuid-unknown" onRowClick={handleClick} />
    </MemoryRouter>
  )
  await userEvent.click(screen.getByTestId('pinned-row-empty'))
  expect(handleClick).toHaveBeenCalledWith({ isPlaceholder: true })
})
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
npx vitest run src/components/RankingTable.test.jsx
```
Expected: 신규 2개 FAIL

- [ ] **Step 3: RankingTable.jsx에 플레이스홀더 추가**

`src/components/RankingTable.jsx`의 기존 `{pinnedRow && ...}` 블록 아래에 추가:
```jsx
{highlightPlayerUuid && !pinnedRow && (
  <div
    className={`${styles.pinnedRow} ${styles.pinnedRowEmpty}`}
    data-testid="pinned-row-empty"
    onClick={onRowClick ? () => onRowClick({ isPlaceholder: true }) : undefined}
    style={onRowClick ? { cursor: 'pointer' } : undefined}
  >
    <span className={styles.pinnedRank}>-위</span>
    <span className={`${styles.pinnedName} ${styles.pinnedAffiliation}`}>
      게임에 참여하러 가기
    </span>
    <span className={styles.pinnedAssets}>-원</span>
  </div>
)}
```

- [ ] **Step 4: RankingTable.module.css에 pinnedRowEmpty 스타일 추가**

`src/components/RankingTable.module.css` 파일 끝에 추가:
```css
.pinnedRowEmpty {
  opacity: 0.75;
  font-style: italic;
}
```

- [ ] **Step 5: RankingPage.jsx 수정 — myPlayerUuid 항상 전달 + 플레이스홀더 클릭 처리**

`src/pages/RankingPage.jsx`에서 `<RankingTable ... />` 부분을 수정:
```jsx
<RankingTable
  rows={rows}
  highlightPlayerUuid={myPlayerUuid}
  onRowClick={row => {
    if (!row || row.isPlaceholder) {
      navigate('/team')
      return
    }
    if (row.sessionId && row.playerUuid) {
      navigate(`/result/${row.sessionId}/player/${row.playerUuid}`)
    }
  }}
/>
```

- [ ] **Step 6: 테스트 통과 확인 후 커밋**

```bash
npx vitest run src/components/RankingTable.test.jsx
```
Expected: PASS (전체)

```bash
git add src/components/RankingTable.jsx src/components/RankingTable.module.css src/components/RankingTable.test.jsx src/pages/RankingPage.jsx
git commit -m "feat: show placeholder pinned row when player has no ranking record"
```

---

## Task 7: 개인 페이지 순차 팝업 (4.1)

**Files:**
- Modify: `src/pages/IndividualPage.jsx`

- [ ] **Step 1: isSequential 상태 및 상수 추가**

`IndividualPage.jsx`의 `useState` 선언들 아래에 추가:
```jsx
const [isSequential, setIsSequential] = useState(false)

const POPUP_SEQUENCE = ['job', 'badges', 'realEstate', 'stocks', 'cash']
```

- [ ] **Step 2: 플레이어 로드 시 순차 팝업 자동 시작**

기존 fetch useEffect에서 `setPlayer(me)`와 `setGameState(...)` 부분을 다음으로 교체:
```jsx
setPlayer(me)
const gs = me.gameState ?? defaultGameState()
setGameState(gs)
if (gs.job === null) {
  setIsSequential(true)
  setActivePopup('job')
}
```

- [ ] **Step 3: selectJob을 순차 모드 지원으로 수정**

기존 `selectJob` 함수를 교체:
```jsx
function selectJob(key) {
  const next = { ...gameState, job: key }
  setGameState(next)
  emitState(next)
  if (isSequential) {
    setActivePopup('badges')
  } else {
    setActivePopup(null)
  }
}
```

- [ ] **Step 4: BadgePopup 컴포넌트 추가 및 confirmBadges 함수 추가**

`IndividualPage.jsx` 파일 끝(export 이후)에 `BadgePopup` 컴포넌트 추가:
```jsx
function BadgePopup({ badges, onConfirm, onClose, isSequential }) {
  const [tempBadges, setTempBadges] = useState([...badges])

  function toggle(i) {
    setTempBadges(prev => {
      const next = [...prev]
      next[i] = !next[i]
      return next
    })
  }

  return (
    <div className={styles.overlay}>
      <div className={styles.popup}>
        <div className={styles.popupTitle}>🏅 성공카드</div>
        <div className={styles.badgeGrid}>
          {BADGE_NAMES.map((name, i) => (
            <button key={name} className={styles.badgeBtn} onClick={() => toggle(i)}>
              <img
                src={`/badges/${name}.png`}
                alt={name}
                className={`${styles.badgeImg} ${!tempBadges[i] ? styles.badgeLocked : ''}`}
              />
              {!tempBadges[i] && <span className={styles.lockIcon}>🔒</span>}
            </button>
          ))}
        </div>
        <div className={styles.popupActions}>
          {!isSequential && <button className={styles.cancelBtn} onClick={onClose}>취소</button>}
          <button className={styles.confirmBtn} onClick={() => onConfirm(tempBadges)}>
            {isSequential ? '다음' : '확인'}
          </button>
        </div>
      </div>
    </div>
  )
}
```

`IndividualPage` 컴포넌트 안에 `confirmBadges` 함수 추가:
```jsx
function confirmBadges(newBadges) {
  const next = { ...gameState, badges: newBadges }
  setGameState(next)
  emitState(next)
  if (isSequential) {
    setTempRealEstate({ ...next.realEstate })
    setActivePopup('realEstate')
  } else {
    setActivePopup(null)
  }
}
```

- [ ] **Step 5: confirmRealEstate, confirmStocks, confirmCash 순차 모드 지원**

기존 함수들을 교체:
```jsx
function confirmRealEstate() {
  const next = { ...gameState, realEstate: tempRealEstate, realEstateVisited: true }
  setGameState(next)
  emitState(next)
  if (isSequential) {
    setTempStocks({ ...next.stocks })
    setActivePopup('stocks')
  } else {
    setActivePopup(null)
  }
}

function confirmStocks() {
  const next = { ...gameState, stocks: tempStocks, stocksVisited: true }
  setGameState(next)
  emitState(next)
  if (isSequential) {
    setTempCash(next.cash ?? 0)
    setActivePopup('cash')
  } else {
    setActivePopup(null)
  }
}

function confirmCash() {
  const next = { ...gameState, cash: tempCash }
  setGameState(next)
  emitState(next)
  setActivePopup(null)
  if (isSequential) setIsSequential(false)
}
```

- [ ] **Step 6: JSX에 BadgePopup 렌더링 추가**

기존 popup들 아래에 추가:
```jsx
{activePopup === 'badges' && (
  <BadgePopup
    badges={gameState.badges}
    onConfirm={confirmBadges}
    onClose={() => setActivePopup(null)}
    isSequential={isSequential}
  />
)}
```

QuantityPopup과 CashPopup에도 `isSequential` prop 전달 (다음 Task에서 UI 반영):
```jsx
{activePopup === 'stocks' && tempStocks && (
  <QuantityPopup
    title="📈 주식 보유 수량"
    items={tempStocks}
    labels={STOCK_LABELS}
    colors={STOCK_COLORS}
    onChange={setTempStocks}
    onConfirm={confirmStocks}
    onClose={() => setActivePopup(null)}
    isSequential={isSequential}
  />
)}
{activePopup === 'realEstate' && tempRealEstate && (
  <QuantityPopup
    title="🏠 부동산 보유 수량"
    items={tempRealEstate}
    labels={REAL_ESTATE_LABELS}
    colors={REAL_ESTATE_COLORS}
    onChange={setTempRealEstate}
    onConfirm={confirmRealEstate}
    onClose={() => setActivePopup(null)}
    isSequential={isSequential}
  />
)}
{activePopup === 'cash' && (
  <CashPopup
    value={tempCash}
    onChange={setTempCash}
    onConfirm={confirmCash}
    onClose={() => setActivePopup(null)}
    isSequential={isSequential}
  />
)}
```

- [ ] **Step 7: 수동 테스트 후 커밋**

`npm run dev` 후:
1. 새 플레이어로 개인 페이지 진입 → 직업 팝업 자동 표시 확인
2. 직업 선택 → 성공카드 팝업 → 부동산 → 주식 → 현금 순서 확인
3. 완료 후 팝업 사라지는 것 확인

```bash
git add src/pages/IndividualPage.jsx
git commit -m "feat: add sequential asset input popup flow on first entry"
```

---

## Task 8: 개인 페이지 슬라이더 + 이미지 표시 (4.2)

**Files:**
- Modify: `src/pages/IndividualPage.jsx`
- Modify: `src/pages/IndividualPage.module.css`

- [ ] **Step 1: ESTATE_IMAGES, STOCK_IMAGES 상수 추가**

`IndividualPage.jsx` 상단의 기존 상수들 아래에 추가:
```jsx
const ESTATE_IMAGES = {
  gaon: '가온개미',
  nuri: '누리고양이',
  dami: '다미원숭이',
  maru: '마루수리',
  chorong: '초롱부엉이',
  hani: '하니여우',
}

const STOCK_IMAGES = {
  semiconductor: '반도체IT',
  finance: '금융산업',
  industrial: '산업재기계',
  auto: '소재화학',
  bio: '바이오헬스케어',
  content: '콘텐츠소비재',
}
```

- [ ] **Step 2: QuantityPopup을 슬라이더 + 이미지 표시로 교체**

기존 `QuantityPopup` 함수를 전체 교체:
```jsx
function QuantityPopup({ title, items, labels, colors, imageFolder, imageMap, onChange, onConfirm, onClose, isSequential }) {
  function handleSlider(key, value) {
    onChange(prev => ({ ...prev, [key]: Number(value) }))
  }
  return (
    <div className={styles.overlay}>
      <div className={styles.popup}>
        <div className={styles.popupTitle}>{title}</div>
        <div className={styles.quantityList}>
          {Object.keys(labels).map(key => (
            <div key={key} className={styles.quantityItem}>
              <span className={styles.quantityLabel}>{labels[key]}</span>
              <div className={styles.sliderRow}>
                <input
                  type="range"
                  min={0}
                  max={10}
                  step={1}
                  value={items[key]}
                  onChange={e => handleSlider(key, e.target.value)}
                  className={styles.slider}
                />
                <span className={styles.sliderCount}>{items[key]}</span>
              </div>
              {imageMap && items[key] > 0 && (
                <div className={styles.assetImgRow}>
                  {Array.from({ length: items[key] }, (_, i) => (
                    <img
                      key={i}
                      src={`/badges/${imageFolder}/${imageMap[key]}.png`}
                      alt={labels[key]}
                      className={styles.assetImgSmall}
                    />
                  ))}
                </div>
              )}
            </div>
          ))}
        </div>
        <div className={styles.popupActions}>
          {!isSequential && <button className={styles.cancelBtn} onClick={onClose}>취소</button>}
          <button className={styles.confirmBtn} onClick={onConfirm}>
            {isSequential ? '다음' : '확인'}
          </button>
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 3: QuantityPopup 호출부에 imageFolder, imageMap prop 추가**

`activePopup === 'realEstate'` 블록의 `<QuantityPopup>`에 추가:
```jsx
imageFolder="estate"
imageMap={ESTATE_IMAGES}
```

`activePopup === 'stocks'` 블록의 `<QuantityPopup>`에 추가:
```jsx
imageFolder="stock"
imageMap={STOCK_IMAGES}
```

- [ ] **Step 4: IndividualPage.module.css에 슬라이더 + 이미지 스타일 추가**

`src/pages/IndividualPage.module.css` 파일 끝에 추가:
```css
.sliderRow {
  display: flex;
  align-items: center;
  gap: 8px;
  width: 100%;
  margin-top: 4px;
}

.slider {
  flex: 1;
  accent-color: #e91e63;
  cursor: pointer;
}

.sliderCount {
  min-width: 20px;
  text-align: right;
  font-size: 14px;
  font-weight: 600;
  color: #fff;
}

.assetImgRow {
  display: flex;
  flex-wrap: wrap;
  gap: 4px;
  margin-top: 6px;
}

.assetImgSmall {
  width: 28px;
  height: 28px;
  object-fit: contain;
  border-radius: 4px;
}
```

- [ ] **Step 5: 수동 테스트 후 커밋**

`npm run dev` 후:
1. 부동산/주식 팝업에서 슬라이더 드래그 → 수치 변경 확인
2. 보유량만큼 이미지 표시 확인
3. 0으로 줄이면 이미지 사라지는 것 확인

```bash
git add src/pages/IndividualPage.jsx src/pages/IndividualPage.module.css
git commit -m "feat: replace +/- buttons with slider and asset image display in QuantityPopup"
```

---

## Task 9: 개인 페이지 현금 키패드 (4.2)

**Files:**
- Modify: `src/pages/IndividualPage.jsx`
- Modify: `src/pages/IndividualPage.module.css`

- [ ] **Step 1: CashPopup을 키패드 UI로 교체**

기존 `CashPopup` 함수를 전체 교체:
```jsx
function CashPopup({ value, onChange, onConfirm, onClose, isSequential }) {
  const [display, setDisplay] = useState(value === 0 ? '' : String(value))

  function handleKey(key) {
    if (key === '←') {
      setDisplay(prev => prev.slice(0, -1))
    } else if (key === '00') {
      setDisplay(prev => (prev === '' ? '' : prev + '00'))
    } else {
      setDisplay(prev => (prev.length >= 10 ? prev : prev + key))
    }
  }

  useEffect(() => {
    onChange(Number(display) || 0)
  }, [display])

  const KEYS = ['1', '2', '3', '4', '5', '6', '7', '8', '9', '00', '0', '←']

  return (
    <div className={styles.overlay}>
      <div className={styles.popup}>
        <div className={styles.popupTitle}>💵 현금</div>
        <div className={styles.cashDisplay}>
          {(Number(display) || 0).toLocaleString()}원
        </div>
        <div className={styles.keypad}>
          {KEYS.map(k => (
            <button key={k} className={styles.keypadBtn} onClick={() => handleKey(k)}>
              {k}
            </button>
          ))}
        </div>
        <div className={styles.popupActions}>
          {!isSequential && <button className={styles.cancelBtn} onClick={onClose}>취소</button>}
          <button className={styles.confirmBtn} onClick={onConfirm}>확인</button>
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: IndividualPage.module.css에 키패드 스타일 추가**

`src/pages/IndividualPage.module.css` 파일 끝에 추가:
```css
.keypad {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 8px;
  margin: 12px 0;
}

.keypadBtn {
  padding: 14px;
  border: 1px solid rgba(255, 255, 255, 0.3);
  border-radius: 8px;
  background: rgba(255, 255, 255, 0.15);
  color: #fff;
  font-size: 18px;
  font-weight: 600;
  cursor: pointer;
}

.keypadBtn:hover {
  background: rgba(255, 255, 255, 0.25);
}
```

주의: 기존 `.cashDisplay`가 이미 파일에 존재할 수 있음. 존재하면 새로 추가하지 말고 기존 스타일을 아래로 덮어쓰거나 병합:
```css
.cashDisplay {
  font-size: 28px;
  font-weight: 700;
  color: #fff;
  text-align: center;
  margin: 12px 0;
  min-height: 40px;
  letter-spacing: 1px;
}
```

- [ ] **Step 3: 수동 테스트 후 커밋**

`npm run dev` 후:
1. 현금 섹션 클릭 → 숫자 키패드 팝업 확인
2. 숫자 누르면 금액 표시 확인 (천 단위 구분)
3. `←` 버튼으로 지우기 확인
4. 확인 후 현금 반영 확인

```bash
git add src/pages/IndividualPage.jsx src/pages/IndividualPage.module.css
git commit -m "feat: replace cash adjustment buttons with numeric keypad"
```

---

## Task 10: 개인 페이지 새로고침 버그 수정 (4.3)

**Files:**
- Modify: `src/pages/Home.jsx`
- Modify: `src/pages/IndividualPage.jsx`

- [ ] **Step 1: Home.jsx에서 join-room 성공 시 localStorage에 프로필 저장**

`src/pages/Home.jsx`의 `joinRoom` 함수를 수정:
```jsx
function joinRoom(code, isHost) {
  const playerUuid = getPlayerUuid()
  socket.emit('join-room', { code, name, affiliation, character, isHost, playerUuid }, ({ ok, error }) => {
    if (ok) {
      localStorage.setItem('player_profile', JSON.stringify({ name, affiliation, character, roomCode: code }))
      navigate(`/lobby/${code}`)
    } else {
      alert(error)
    }
  })
}
```

- [ ] **Step 2: IndividualPage.jsx의 플레이어 탐색 로직에 재연결 처리 추가**

기존 fetch useEffect 전체를 교체:
```jsx
useEffect(() => {
  if (!socket) return

  fetch(`/api/rooms/${code}`)
    .then(r => r.json())
    .then(data => {
      const me = data.players?.find(p => p.socketId === socket.id)

      if (me) {
        setPlayer(me)
        const gs = me.gameState ?? defaultGameState()
        setGameState(gs)
        if (gs.job === null) {
          setIsSequential(true)
          setActivePopup('job')
        }
        return
      }

      // 재연결: localStorage 프로필로 join-room 재시도
      let profile = null
      try {
        const raw = localStorage.getItem('player_profile')
        if (raw) profile = JSON.parse(raw)
      } catch (_) {}

      const playerUuid = localStorage.getItem('player_uuid')

      if (!profile || !playerUuid || profile.roomCode !== code) {
        navigate(`/lobby/${code}`)
        return
      }

      socket.emit('join-room', {
        code,
        name: profile.name,
        affiliation: profile.affiliation,
        character: profile.character,
        isHost: false,
        playerUuid,
      }, res => {
        if (!res?.ok) { navigate(`/lobby/${code}`); return }

        fetch(`/api/rooms/${code}`)
          .then(r => r.json())
          .then(data2 => {
            const rejoined = data2.players?.find(p => p.socketId === socket.id)
            if (!rejoined) { navigate(`/lobby/${code}`); return }
            setPlayer(rejoined)
            const gs = rejoined.gameState ?? defaultGameState()
            setGameState(gs)
            if (gs.job === null) {
              setIsSequential(true)
              setActivePopup('job')
            }
          })
          .catch(() => navigate(`/lobby/${code}`))
      })
    })
    .catch(() => navigate(`/lobby/${code}`))
}, [code, socket])
```

- [ ] **Step 3: 수동 테스트**

`npm run dev` 후:
1. 플레이어로 방 입장 → 개인 페이지 이동
2. 브라우저 새로고침 (F5)
3. 이름/캐릭터/소속이 유지된 채로 개인 페이지가 표시되는지 확인
4. 새로고침 후에도 로비 플레이어 목록에 표시되는지 확인

- [ ] **Step 4: 전체 테스트 후 커밋**

```bash
npm test -- --run
```
Expected: PASS

```bash
git add src/pages/Home.jsx src/pages/IndividualPage.jsx
git commit -m "fix: persist player profile to localStorage and reconnect on IndividualPage refresh"
```

---

## 최종 검증

- [ ] `npm test -- --run` — 전체 테스트 PASS 확인
- [ ] `npm run build` — 빌드 에러 없음 확인
- [ ] 브라우저에서 전체 플로우 수동 테스트:
  1. 뒤로가기 버튼이 모든 화면 왼쪽 상단에 표시되고 이전 화면으로 이동
  2. 방장이 팀원 X 버튼으로 추방, 추방된 플레이어가 `/team`으로 이동
  3. 랭킹 탭이 "전체" / "소속" / "팀"으로 표시
  4. 내 기록 없을 때 "-위 게임에 참여하러 가기 -원" 플레이스홀더 행 표시
  5. 개인 페이지 최초 진입 시 직업→성공카드→부동산→주식→현금 순서로 팝업
  6. 주식/부동산 슬라이더 + 이미지 표시
  7. 현금 키패드 입력
  8. 개인 페이지 새로고침 후 이름/캐릭터 유지
